apiVersion: v1
kind: Secret
metadata:
  name: {{ include "web.fullname" . }}-image-registry-secret
  namespace: {{ .Values.namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "imagePullSecret" . }}
